{
  "name": "selfhealer-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Repo de demonstração: testes Playwright com o reporter do Self-Healer.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "@selfhealer/playwright": "file:./vendor/selfhealer-playwright"
  }
}
